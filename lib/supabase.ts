// Supabase client — createServerClient / createBrowserClient
// Re-export from @supabase/ssr in production

export function createClient(url: string, key: string) {
  // Placeholder — implement with @supabase/ssr
  return null as any;
}