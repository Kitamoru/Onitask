/**
 * Feature flags — временные и постоянные переключатели поведения UI.
 *
 * TASK_SWIPE_ENABLED=false (2026-09-13, SUBMIT-01):
 *  Свайпы карточек между колонками временно отключены — ждём единой стратегии
 *  (returns путают, жест работал частично, негативный UX). Код свайпа
 *  (SwipeableTaskCard) НЕ удалён — гейт находится на canMoveNext/canMovePrev,
 *  потому что onTap живёт внутри тех же touch/mouse-хендлеров (слабый свайп = тап),
 *  поэтому простое снятие хендлеров сломало бы открытие карточки.
 *  Ре-включение: поставить true и прогнать чек-лист:
 *    - свайп вправо двигает in_progress → review (и т.д. по CCOLUMN_ORDER);
 *    - свайп влево возвращает;
 *    - тап по карточке открывает задачу;
 *    - сдача результата (форма «Результат») не конфликтует с жестом.
 */
export const TASK_SWIPE_ENABLED = false;